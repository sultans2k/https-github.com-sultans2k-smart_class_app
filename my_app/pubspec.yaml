name: smart_class # Updated name
description: "AI-powered teaching assistant."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  speech_to_text: ^7.3.0
  google_generative_ai: ^0.4.6
  audioplayers: ^6.0.0
  http: ^1.6.0
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  uuid: ^4.4.0
  path_provider: ^2.1.2
  flutter_sound: ^9.2.13
  permission_handler: ^11.3.1
  file_picker: ^8.0.0
  syncfusion_flutter_pdf: ^25.1.35   # free community license, best text extraction
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  google_sign_in: ^7.2.0
  cloud_firestore: ^6.3.0
  firebase_messaging: ^16.0.4
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# This block MUST be at the top level (left-aligned)

flutter:
  uses-material-design: true

  